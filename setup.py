# encoding=utf-8

from setuptools import setup

setup(name='gen_code_for_structure_reader',
      version='0.1',
      description='A tool to generate structure_reader.structure from C/C++ code',
      url='http://github.com/abc19899@foxmail.com/gen_code_for_structure_reader/',
      author='Shiqiang Zhang',
      author_email='abc19899@foxmail.com',
      license='MIT',
      packages=['gen_code_for_structure_reader'],
      zip_safe=False,

      # setup_requires=['pytest-runner', ],
      # tests_require=['pytest', ],

      )
